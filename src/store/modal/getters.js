export const getters = {
  getModal: (state) => state.modal,
  getModalConfirm: (state) => state.modalConfirm,
  getModalOrderr: (state) => state.modalOrder,
  getmodalOrder: (state) => {
    return state.modalOrder;
  },
};
